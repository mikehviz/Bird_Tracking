# Bird_Tracking

- Included is the data set, a static pdf file which shows the matplot outputs since github does not support them and the code file itself which tracks the birds migration over night and day.
